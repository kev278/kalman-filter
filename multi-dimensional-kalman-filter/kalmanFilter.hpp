#include <Eigen/Dense>
#include <vector>

class kalmanFilter
{
    std::vector<double> measurements;

    public:

        void filter(Eigen::MatrixXd x, Eigen::MatrixXd P)
        {
            
        }
};